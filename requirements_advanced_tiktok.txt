# Advanced TikTok Bypass Dependencies
# Install with: pip install -r requirements_advanced_tiktok.txt

# Core dependencies (already in main requirements.txt)
yt-dlp>=2023.12.30
requests>=2.31.0

# Browser impersonation for bypassing detection
curl-cffi>=0.6.0
impersonate>=0.1.0

# Browser automation for direct extraction
selenium>=4.15.0
webdriver-manager>=4.0.0

# Alternative downloaders
gallery-dl>=1.26.0

# Proxy support
requests[socks]>=2.31.0
pysocks>=1.7.1

# Additional HTTP libraries for different approaches
httpx>=0.25.0
aiohttp>=3.9.0

# User agent rotation
fake-useragent>=1.4.0

# Optional: For advanced cookie handling
browser-cookie3>=0.19.0

# Optional: For captcha solving (if needed)
# 2captcha-python>=1.1.3
# anticaptchaofficial>=1.0.46

# Note: Some dependencies may require additional system packages:
# - Chrome/Chromium browser for Selenium
# - FFmpeg for video processing (already required by main app)